# `lazy.nvim` Setup for Neovim

## Installation

```bash
mv ~/.config/nvim{,.bkp}
git clone -b main https://github.com/mimanshu-maheshwari/setup-nvim-lazy.git ~/.config/nvim
rm -rf ~/.config/nvim/.git
```

## Modification 

- Add more plugins in `./lua/plugins/` path.

